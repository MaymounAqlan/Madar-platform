# دليل تشغيل منصة MADAR محلياً
# MADAR Local Setup Guide

**آخر تحديث:** 2026-07-04

---

## المتطلبات المسبقة

قبل البدء، تأكد من تثبيت البرامج التالية على جهازك:

| البرنامج | الإصدار المطلوب | رابط التحميل |
|---------|----------------|-------------|
| **Docker Desktop** | 4.30+ | https://www.docker.com/products/docker-desktop |
| **Docker Compose** | 2.20+ | يأتي مع Docker Desktop |
| **Node.js** | 20+ | https://nodejs.org (للتشغيل اليدوي) |
| **Git** | 2.40+ | https://git-scm.com |

> **ملاحظة:** Docker Desktop يتضمن Docker Compose تلقائياً على Windows وMac.

---

## الطريقة الأولى: التشغيل باستخدام Docker Compose (مستحسن)

هذه أسرع طريقة — تشغل كل الخدمات بأمر واحد.

### الخطوة 1: تحميل المشروع

```bash
# انتقل لمجلد المشروع
cd /mnt/agents/output

# أو إذا كنت قد نقلت الملفات لمكان آخر
cd /path/to/madar
```

### الخطوة 2: التحقق من ملفات المشروع

```bash
# تأكد من وجود هذه الملفات
ls docker-compose.yml .env nginx/nginx.conf
ls madar-backend/Dockerfile madar-backend/package.json
ls madar-ai/Dockerfile madar-ai/requirements.txt
ls scripts/seed-data.js scripts/mongo-init.js
```

### الخطوة 3: تشغيل جميع الخدمات

```bash
# التشغيل لأول مرة (يبني الصور ويُشغل)
docker compose up -d --build

# إذا كنت قد شغلت من قبل (بدون بناء)
docker compose up -d
```

> **الأول مرة** تستغرق حوالي 5-10 دقائق حسب سرعة الإنترنت لأنها:
> - تحميل صور MongoDB + Redis + Nginx
> - بناء صورة Backend (NestJS)
> - بناء صورة AI Engine (Python)
> - تحميل نموذج all-MiniLM-L6-v2 (~80 ميجا)

### الخطوة 4: التحقق من حالة الخدمات

```bash
# عرض حالة جميع الحاويات
docker compose ps
```

يجب أن ترى شيئاً مشابهاً:

```
NAME           STATUS    PORTS
madar-nginx    Up        0.0.0.0:80->80/tcp
madar-backend  Up        0.0.0.0:3001->3001/tcp
madar-ai       Up        0.0.0.0:8000->8000/tcp
madar-mongodb  Up        0.0.0.0:27017->27017/tcp
madar-redis    Up        0.0.0.0:6379->6379/tcp
```

### الخطوة 5: ملء قاعدة البيانات بالبيانات التجريبية (Seed Data)

```bash
# تثبيت حزم Node.js المطلوبة للـ seed
cd scripts
npm install mongodb bcryptjs

# تشغيل سكربت ملء البيانات
node seed-data.js
```

سيطبع السكربت:
```
========================================
  Seed completed successfully!
========================================

Login credentials for testing:
  Admin:     admin@madar.sa / Madar@2024
  Student 1: ahmed@student.ksu.edu.sa / Madar@2024
  Student 2: sara@student.kau.edu.sa / Madar@2024
  ...
```

### الخطوة 6: فتح المنصة في المتصفح

```
الواجهة الأمامية:  http://localhost
Backend API:       http://localhost/api
Swagger Docs:      http://localhost/api/docs
AI Engine Docs:     http://localhost/api/ai/docs
```

### الخطوة 7: التحقق من API

```bash
# فحص صحة Backend
curl http://localhost/api/health

# فحص صحة AI Engine
curl http://localhost/api/ai/health

# فحص وثائق API (Swagger)
# افتح في المتصفح: http://localhost/api/docs
```

---

## الحسابات الافتراضية للاختبار

بعد تشغيل سكربت `seed-data.js`، استخدم هذه الحسابات:

| الدور | البريد الإلكتروني | كلمة المرور |
|-------|------------------|-------------|
| **مسؤول النظام** | `admin@madar.sa` | `Madar@2024` |
| **طالب (KSU - علوم حاسب)** | `ahmed@student.ksu.edu.sa` | `Madar@2024` |
| **طالب (KAU - هندسة برمجيات)** | `sara@student.kau.edu.sa` | `Madar@2024` |
| **طالب (KFUPM - هندسة حاسب)** | `khalid@student.kfupm.edu.sa` | `Madar@2024` |
| **طالب (KSU - نظم معلومات)** | `noura@student.imamu.edu.sa` | `Madar@2024` |
| **طالب (KFUPM - علوم حاسب)** | `fahad@student.ksu.edu.sa` | `Madar@2024` |
| **شركة (أرامكو)** | `hr@aramco.com` | `Madar@2024` |
| **شركة (STC)** | `recruitment@stc.com.sa` | `Madar@2024` |
| **شركة (سابك)** | `careers@sabic.com` | `Madar@2024` |
| **شركة (أكوا باور)** | `hr@acwa.com` | `Madar@2024` |
| **شركة (نيوم)** | `jobs@neom.com` | `Madar@2024` |
| **جامعة (الملك سعود)** | `cs@ksu.edu.sa` | `Madar@2024` |
| **جامعة (الملك فهد)** | `it@kfupm.edu.sa` | `Madar@2024` |
| **جامعة (الملك عبدالعزيز)** | `admin@kau.edu.sa` | `Madar@2024` |
| **منسق (الملك سعود)** | `coordinator@ksu.edu.sa` | `Madar@2024` |

---

## الطريقة الثانية: التشغيل اليدوي (للمطورين)

إذا كنت تريد تطوير المشروع وتعديل الكود.

### 1️⃣ تشغيل MongoDB

```bash
# باستخدام Docker
docker run -d --name madar-mongo \
  -p 27017:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=admin \
  -e MONGO_INITDB_ROOT_PASSWORD=madar_secure_2024 \
  -v mongo_data:/data/db \
  mongo:7.0

# أو إذا كنت تفضل MongoDB Compass (GUI)
# حمله من: https://www.mongodb.com/products/compass
```

### 2️⃣ تشغيل Redis

```bash
# باستخدام Docker
docker run -d --name madar-redis \
  -p 6379:6379 \
  redis:7-alpine
```

### 3️⃣ تشغيل Backend (NestJS)

```bash
cd madar-backend

# تثبيت الحزم
npm install

# إنشاء ملف البيئة
cat > .env << 'EOF'
PORT=3001
MONGODB_URI=mongodb://admin:madar_secure_2024@localhost:27017/madar?authSource=admin
REDIS_URL=redis://localhost:6379
JWT_SECRET=madar_super_secret_key_change_this
JWT_REFRESH_SECRET=madar_refresh_secret_change_this
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
FRONTEND_URL=http://localhost:3000
AI_SERVICE_URL=http://localhost:8000
SWAGGER_ENABLED=true
EOF

# التشغيل في وضع التطوير
npm run start:dev

# أو البناء والتشغيل
npm run build
npm run start:prod
```

> **الـ Backend** يعمل على: http://localhost:3001  
> **وثائق API (Swagger):** http://localhost:3001/api/docs

### 4️⃣ تشغيل AI Engine (Python)

```bash
cd madar-ai

# إنشاء بيئة Python افتراضية
python3 -m venv venv

# تفعيل البيئة
# على Linux/Mac:
source venv/bin/activate
# على Windows:
venv\Scripts\activate

# تثبيت الحزم
pip install -r requirements.txt

# التشغيل
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

> **ملاحظة:** أول مرة ستحمّل نموذج all-MiniLM-L6-v2 (~80 ميجا) — قد تستغرق 2-5 دقائق.  
> **الـ AI Engine** يعمل على: http://localhost:8000

### 5️⃣ تشغيل Frontend (React)

```bash
cd app

# تثبيت الحزم
npm install

# إنشاء ملف البيئة
echo "VITE_API_URL=http://localhost:3001" > .env

# التشغيل في وضع التطوير
npm run dev

# أو البناء
npm run build
```

> **الـ Frontend** يعمل على: http://localhost:5173 (dev) أو http://localhost:80 (build + nginx)

### 6️⃣ ملء البيانات التجريبية (Seed Data)

```bash
cd scripts
npm install mongodb bcryptjs
node seed-data.js
```

---

## أوامر Docker المفيدة

```bash
# عرض السجلات (Logs)
docker compose logs -f              # جميع الخدمات
docker compose logs -f backend      # Backend فقط
docker compose logs -f ai-engine    # AI Engine فقط
docker compose logs -f mongodb      # MongoDB فقط
docker compose logs -f nginx        # Nginx فقط
docker compose logs -f redis        # Redis فقط

# إعادة تشغيل خدمة معينة
docker compose restart backend
docker compose restart ai-engine

# الدخول لحاوية معينة
docker compose exec backend sh
docker compose exec mongodb mongosh

# إيقاف جميع الخدمات (يحفظ البيانات)
docker compose down

# إيقاف + حذف البيانات (⚠️ احذر!)
docker compose down -v

# إعادة البناء بعد تعديل الكود
docker compose up -d --build

# عرض استهلاك الموارد
docker stats

# إعادة ضبط كل شيء من الصفر
docker compose down -v
docker compose up -d --build
cd scripts && node seed-data.js
```

---

## حل المشاكل الشائعة

### ❌ خطأ: Port already in use (المنفذ مستخدم)
```
Error: Ports are not available: exposing port TCP 0.0.0.0:80
```
**الحل:** أغلق البرنامج الذي يستخدم المنفذ 80 (مثل Apache, Nginx, IIS) أو عدّل المنافذ في `docker-compose.yml`:
```yaml
ports:
  - "8080:80"  # غير 80 لـ 8080
```

### ❌ خطأ: AI Engine بطيء في التشغيل
**الحل:** طبيعي — أول مرة يحمل النموذج (~80 ميجا). انتظر 2-5 دقائق.  
تحقق من التحميل:
```bash
docker compose logs -f ai-engine
```

### ❌ خطأ: MongoDB connection refused
**الحل:** تأكد من تشغيل MongoDB:
```bash
docker compose ps mongodb
docker compose logs mongodb
```
إذا استمرت المشكلة، تأكد من صحة MONGODB_URI في ملف `.env`.

### ❌ خطأ: Cannot find module (Backend)
**الحل:** أعد تثبيت الحزم:
```bash
cd madar-backend && rm -rf node_modules package-lock.json && npm install
```

### ❌ خطأ: Backend build fails
**الحل:** تأكد من وجود nest-cli.json و tsconfig.json:
```bash
ls madar-backend/nest-cli.json madar-backend/tsconfig.json
# إذا ناقصة:
cp madar-backend/package.json /tmp/ && cd madar-backend && npx @nestjs/cli new . --skip-git
```

### ❌ خطأ: Page not found عند تحديث الصفحة
**الحل:** طبيعي في SPA — تأكد من تفعيل try_files في nginx.conf:
```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

### ❌ خطأ: Docker image build fails (backend)
**الحل:** قد تحتاج زيادة موارد Docker:
- Docker Desktop > Settings > Resources > Memory: 4GB+
- أو ملف `.wslconfig` على Windows:
```ini
[wsl2]
memory=4GB
processors=2
```

### ❌ خطأ: Redis connection failed
**الحل:** تأكد من تشغيل Redis:
```bash
docker compose ps redis
docker compose exec redis redis-cli ping
# يجب أن يرد: PONG
```

### ❌ خطأ: 502 Bad Gateway من Nginx
**الحل:** Backend أو AI Engine غير جاهزين بعد. تحقق:
```bash
docker compose ps
# انتظر حتى تصبح HEALTHY

# أو تحقق من السجلات
docker compose logs backend | tail -20
docker compose logs ai-engine | tail -20
```

---

## هيكل المشروع

```
madar/
├── docker-compose.yml          ← تشغيل كل الخدمات (5 خدمات)
├── .env                        ← متغيرات البيئة
├── README.md                   ← دليل شامل بالإنجليزية
├── nginx/
│   └── nginx.conf              ← توجيه الطلبات + Rate Limiting
├── scripts/
│   ├── mongo-init.js           ← تهيئة أولية لقاعدة البيانات
│   └── seed-data.js            ← بيانات تجريبية (15 مستخدم + 12 وظيفة + ...)
├── madar-backend/              ← NestJS Backend API
│   ├── Dockerfile              ← إصلاح: PORT 3001 + npm install fallback
│   ├── package.json            ← 25+ تبعية
│   ├── nest-cli.json           ← إعدادات NestJS
│   ├── tsconfig.json           ← إعدادات TypeScript
│   └── src/
│       ├── auth/               ← JWT + RBAC (6 أدوار)
│       ├── students/           ← بوابة الطالب
│       ├── companies/          ← بوابة الشركة
│       ├── universities/       ← بوابة الجامعة
│       ├── jobs/               ← الوظائف
│       ├── applications/       ← الطلبات
│       ├── matching/           ← التطابق + Bull Queue
│       ├── analytics/          ← التحليلات
│       └── common/             ← أدوات مشتركة (إشعارات، سجلات، رسائل)
├── madar-ai/                   ← Python AI Engine
│   ├── Dockerfile              ← Multi-stage build + non-root user
│   ├── requirements.txt        ← 20+ حزمة Python
│   ├── main.py                 ← نقطة دخول FastAPI
│   ├── config.py               ← إعدادات قابلة للتهيئة
│   ├── models/                 ← NLP + Matching + Embeddings
│   ├── routers/                ← نقاط النهاية (API Endpoints)
│   ├── services/               ← منطق الأعمال
│   └── utils/                  ← أدوات مساعدة
└── app/                        ← React Frontend
    ├── dist/                   ← ملفات البناء الإنتاجي
    ├── src/
    │   ├── pages/              ← صفحات المنصة (16+ صفحة)
    │   ├── components/         ← مكونات مشتركة
    │   ├── data/               ← بيانات وهمية
    │   └── services/           ← ربط API
    └── package.json
```

---

## الخدمات والمنافذ

| الخدمة | المنفذ | الوصف |
|--------|--------|-------|
| **Nginx** | `80` | بروكسي عكسي + ملفات ثابتة |
| **Backend API** | `3001` | NestJS REST API (داخلي) |
| **AI Engine** | `8000` | Python FastAPI (داخلي) |
| **MongoDB** | `27017` | قاعدة البيانات الرئيسية (داخلي) |
| **Redis** | `6379` | التخزين المؤقت + الطوابير (داخلي) |

### توجيه API عبر Nginx

| المسار | الخدمة المستهدفة | الغرض |
|--------|----------------|-------|
| `/` | Frontend static | واجهة المنصة |
| `/api/*` | NestJS Backend | REST API |
| `/api/ai/*` | Python AI Engine | خدمات الذكاء الاصطناعي |

---

## ملخص سريع (Quick Start)

```bash
# 1. انتقل لمجلد المشروع
cd /mnt/agents/output

# 2. شغّل كل الخدمات
docker compose up -d --build

# 3. انتظر 3-5 دقائق (أول مرة)

# 4. املأ قاعدة البيانات
cd scripts && npm install mongodb bcryptjs && node seed-data.js

# 5. افتح المتصفح
#    http://localhost

# 6. سجّل الدخول
#    البريد: admin@madar.sa
#    كلمة المرور: Madar@2024

# ✅ جاهز!
```

---

## ملاحظات أمان مهمة

1. **غيّر كلمات المرور** قبل النشر في الإنتاج
2. **حدّث `JWT_SECRET`** و `JWT_REFRESH_SECRET` في ملف `.env`
3. **فعّل HTTPS** في الإنتاج (أضف شهادات SSL في `nginx/ssl/`)
4. **لا تشغّل** Swagger (`SWAGGER_ENABLED=false`) في الإنتاج
5. **استخدم** MongoDB authentication في الإنتاج (مُفعّل افتراضياً)

---

**للدعم الفني:** support@madar.sa  
**الإصدار:** 1.0.0  
**آخر تحديث:** 2026-07-04
